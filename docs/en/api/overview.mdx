---
title: API overview
description: Arona API gateway endpoints (sub2api)
---

# API overview

## Base URL

```text
https://sub2api.lingshichat.top/v1
```

## Common endpoints

| Method | Path | Description |
| --- | --- | --- |
| `GET` | `/v1/models` | List models for the current key |
| `POST` | `/v1/chat/completions` | OpenAI Chat Completions |
| `POST` | `/v1/responses` | OpenAI Responses |
| `POST` | `/v1/messages` | Anthropic Messages |
| `POST` | `/v1/embeddings` | Embeddings (if platform supports) |
| `POST` | `/v1/images/generations` | Image generation (if platform supports) |

## Auth

```bash
Authorization: Bearer YOUR_API_KEY
```

## Recommended model

```text
grok-4.5
```

## Minimal example

```bash
curl https://sub2api.lingshichat.top/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "grok-4.5",
    "messages": [{"role": "user", "content": "hello"}]
  }'
```

## Client setup

If you configure a CLI rather than raw HTTP:

- [Grok Build](/en/clients/grok-build)
- [Codex](/en/clients/codex)
- [Claude Code](/en/clients/claude-code)
- [OpenCode](/en/clients/opencode)
- [Pi Agent](/en/clients/pi-agent)
